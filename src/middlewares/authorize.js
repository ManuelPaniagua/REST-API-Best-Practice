// const authorize = (req, res, next) => {
//     const user = req.user;
  
//     // Example: Checking if the user has an admin role
//     if (user.role !== "admin") {
//       return res.status(403).send({ status: "FAILED", data: { error: "Access denied" } });
//     }
  
//     next();
//   };
  
//   module.exports = authorize;
  